Empty directories doesn't deploy on Heroku, that's the reason for this file.

We also added the following rules to the `.gitignore` file:

# Customers photos
public/images/customers/*
!public/images/customers/.deploy-empty-folder-on-heroku